
http {
  server {
  
    error_log  /home/box/web/error_log info;
    location / {
      deny  all;
    }
  
    location ^~ /uploads/ {
      alias /home/box/web/uploads;
    }
    
    location ~ \*.*$ {
      root /home/box/web/public;
    }
  }
}
